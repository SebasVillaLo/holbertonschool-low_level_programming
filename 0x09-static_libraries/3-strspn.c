#include "holberton.h"
#include <stdio.h>
/**
* _strspn - Funtion
* @s: char
* @accept: char
* Return: Always 0 (Success)
*/
unsigned int _strspn(char *s, char *accept)
{
	unsigned int acumulator = 0;

	s = (s + acumulator);
	accept = (accept + acumulator);

	return (acumulator);
}
