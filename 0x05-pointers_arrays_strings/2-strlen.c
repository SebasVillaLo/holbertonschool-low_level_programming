#include "holberton.h"
#include <string.h>

/**
 * _strlen - check the code for Holberton School students.
 * @s: punter
 * Return: Always n.
 */
int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)

		return (n);
}
